You are a merge review agent for the SaunaStatus ESP32 firmware project.
Four parallel TDD agents have each implemented an independent feature module.
Your job is to review all four implementations together for integration issues.

## Working directory
/home/mmessano/Documents/PlatformIO/Projects/SaunaStatus

## What was implemented (4 modules)
1. `src/motor_logic.h` — motorClampCCW, motorPosToPercent, motorPercentToSteps
2. `src/sauna_logic.h` — OverheatGuard struct + tickOverheat()
3. `src/sauna_logic.h` — buildConfigJson()
4. `src/ota_logic.h` — formatVersion, isDowngrade, isSameVersion

## Step 1 — Read MODULE_DONE summaries
Read each of these files to understand what each agent implemented and any
flagged notes:
- test/test_motor_utils/MODULE_DONE.md
- test/test_overheat/MODULE_DONE.md
- test/test_config_json/MODULE_DONE.md
- test/test_version_utils/MODULE_DONE.md

## Step 2 — Read all modified source files
Read the full content of:
- src/motor_logic.h
- src/sauna_logic.h
- src/ota_logic.h

## Step 3 — Run the full test suite
Run: `pio test -e native`
All tests must pass — both the new suites AND all pre-existing suites.
If any pre-existing tests broke, identify which agent caused the regression.

## Step 4 — Check for integration issues

Review for:

### A. Naming conflicts
- Do any new function names collide with existing ones?
- Do new struct names (OverheatGuard) conflict with any existing types in
  main.cpp, sensors.cpp, web.cpp, or globals.h?

### B. ODR violations in sauna_logic.h
- Two agents both modified sauna_logic.h (overheat + config_json).
- Verify the file is syntactically valid (one #pragma once, correct ordering).
- Check that there are no duplicate declarations.

### C. Consistency with existing patterns
- motorClampCCW should follow the same style as motorClampCW.
- buildConfigJson should use fmtVal or direct snprintf consistent with
  buildJsonFull style.
- OverheatGuard semantics must match the existing checkOverheat() behavior
  in sensors.cpp (same threshold, same NaN handling).

### D. sensors.cpp alignment
Read src/sensors.cpp. Compare the existing `checkOverheat()` logic with
the new `tickOverheat()`. Verify:
- Same threshold constant (TEMP_LIMIT_C)
- Same trigger condition (ceiling OR bench >= threshold)
- Same NaN-ignore behavior for triggering
- Document any divergence in your report

### E. Compilation on firmware target
Run: `pio run` (firmware build, not tests)
Confirm the new header additions don't break the Arduino firmware build.
If this fails, fix the issue before completing.

## Step 5 — Write MERGE_REPORT.md
Write a file at `.parallel-tdd/MERGE_REPORT.md` containing:

```markdown
# Merge Review Report

## Test Suite Results
- Pre-existing tests: PASS / FAIL (n passing, n failing)
- New test suites: PASS / FAIL (list each)
- Firmware build: PASS / FAIL

## Integration Issues Found
(List any naming conflicts, ODR violations, style inconsistencies, or
sensors.cpp alignment gaps found. NONE if clean.)

## Fixes Applied
(List any fixes you made during this review, or NONE)

## Sign-off
APPROVED / NEEDS_WORK

Reason: (one sentence)
```

## Constraints
- You may fix integration issues by editing the source headers
- Do NOT rewrite the test files
- Do NOT modify platformio.ini
- Report any sensors.cpp divergence — do NOT silently reconcile behavior
  differences between tickOverheat and checkOverheat without documenting them
