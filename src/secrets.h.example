#pragma once
// =============================================================================
// SECRETS TEMPLATE — Copy to secrets.h and fill in your real values.
// secrets.h is gitignored and must never be committed.
// =============================================================================

// WiFi
#define WIFI_SSID     "your_ssid"
#define WIFI_PASSWORD "your_password"

// InfluxDB 2.x
#define INFLUXDB_URL    "http://your-influx-host:8086"
#define INFLUXDB_TOKEN  "your-influxdb-token"
#define INFLUXDB_ORG    "your-org-id"
#define INFLUXDB_BUCKET "sauna_data"

// MQTT broker — leave USER/PASS empty strings if no auth
#define MQTT_BROKER "your-mqtt-host"
#define MQTT_PORT   1883
#define MQTT_USER   ""
#define MQTT_PASS   ""

// Auth credentials for emergency admin account (seeded on first boot if NVS is empty)
// IMPORTANT: Change these before first flash — they cannot be rotated without NVS erase
#define AUTH_ADMIN_USER "admin"
#define AUTH_ADMIN_PASS "change-this-password"
